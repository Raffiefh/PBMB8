import 'package:flutter/material.dart';

class RiwayatContent extends StatelessWidget {
  const RiwayatContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Riwayat Panitia"));
  }
}
