import 'package:flutter/material.dart';

class TiketPeserta extends StatelessWidget {
  const TiketPeserta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Tiket Peserta"));
  }
}
