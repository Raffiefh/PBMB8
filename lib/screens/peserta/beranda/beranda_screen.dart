import 'package:flutter/material.dart';

class BerandaPeserta extends StatelessWidget {
  const BerandaPeserta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Beranda Peseerta"));
  }
}
