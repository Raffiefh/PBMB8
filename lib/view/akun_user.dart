import 'package:flutter/material.dart';

class AkunUser extends StatelessWidget {
  const AkunUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Ini Halaman Akun",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}