import 'package:flutter/material.dart';

class ForumUser extends StatelessWidget {
  const ForumUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini Halaman Forum", style: TextStyle(fontSize: 24)),
    );
  }
}